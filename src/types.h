#pragma once

#include <list>
template <class T>
using List = std::list<T>;

#include <vector>
template <class T>
using Vector = std::vector<T>;

#include <memory>
template <class T>
using SharedPtr = std::shared_ptr<T>;